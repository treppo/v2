image: docker:latest
services:
  - docker:dind

variables:
  DOCKER_DRIVER: overlay

stages:
  - build
  - release

build:
  stage: build
  script:
    - ./build.sh

release:
  stage: release
  script:
    - ./release.sh
